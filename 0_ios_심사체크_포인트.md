# iOS 심사 체크 포인트 (2026 최신 기준)

본 문서는 `koreaTaiwan` 프로젝트의 현재 구현 상태를 기반으로, 최신(2026년) Apple App Store Review Guidelines에 따라 **반드시 확인 및 수정해야 하는 사항**들을 정리한 것입니다.

---

## 🚨 [필수 조치] 주요 심사 반려(Reject) 예상 포인트

### 1. Apple 로그인 필수 (Guideline 4.8)
Apple 정책에 따라, 서드파티 소셜 로그인(구글, 카카오, 네이버 등)을 제공하는 앱은 **반드시 Apple 로그인(Sign in with Apple)을 동등한 수준으로 제공**해야 합니다.
- **현재 상태:** `app.config.js` 및 `components/login/index.js` 확인 결과 카카오, 네이버, 구글 로그인은 구현되어 있으나 Apple 로그인이 없습니다.
- **수정 대상 파일:**
  - `components/login/index.js` (Apple 로그인 UI 버튼 추가)
  - `app.config.js` (expo-apple-authentication 플러그인 추가 및 설정)

### 2. 계정 삭제(회원 탈퇴) 기능 (Guideline 5.1.1(v))
사용자가 생성한 계정을 앱 내에서 직접 삭제(탈퇴)할 수 있는 옵션이 명확하게 제공되어야 합니다. 또한 이 과정에서 관련된 개인 데이터도 모두 삭제되어야 합니다.
- **현재 상태:** `components/mypage/index.js` 등 마이페이지 화면을 확인한 결과, '회원 탈퇴' 기능의 UI 버튼 및 로직이 누락되어 있습니다.
- **수정 대상 파일:**
  - `components/mypage/index.js` (회원 탈퇴 메뉴 및 팝업/확인 로직 추가)
  - `components/mypage/hook/mypageHook.js` (탈퇴 API 호출 로직 추가)

### 3. 사용자 생성 콘텐츠(UGC) 안전 장치 (Guideline 1.2)
커뮤니티 게시판 등 사용자가 콘텐츠를 직접 올릴 수 있는 기능이 있는 경우, 유해 콘텐츠 방지를 위한 시스템이 필수입니다.
- **현재 상태:** `components/community/` 하위 컴포넌트에 신고 및 차단 기능이 없습니다.
- **필수 추가 항목:**
  1. **신고하기:** 불쾌한 게시물이나 댓글을 신고할 수 있는 기능
  2. **차단하기:** 특정 악성 사용자의 글을 안 보이게 차단하는 기능
  3. **EULA (최종사용자 라이선스 약관):** 유해 콘텐츠/사용자에 대한 무관용 원칙(Zero Tolerance) 조항
- **수정 대상 파일:**
  - `components/community/detail.js` (게시물/댓글 우측 상단 메뉴에 '신고', '차단' 추가)
  - `components/terms/policy.js` (UGC 관련 무관용 정책 약관 내용 보강)

---

## 🔍 [일반 조치] 유의 및 점검 사항

### 4. 권한 요청 시 명확한 사유 제공 (Guideline 5.1.1)
카메라, 사진, 위치, 알림 등의 권한을 요청할 때는 **"이 앱에서 이 권한을 왜 필요로 하는지"** 매우 구체적으로 설명해야 합니다. (예: "주변 혜택 매장을 찾기 위해 위치 권한이 필요합니다.")
- **수정 대상 파일:**
  - `app.config.js` 내 `ios.infoPlist` 설정 항목
  - (예: `NSLocationWhenInUseUsageDescription`, `NSCameraUsageDescription` 등)

### 5. 테스트 계정 제공
로그인이 필요한 앱이므로 심사 제출 시 App Store Connect에 **모든 기능을 테스트할 수 있는 데모 계정(ID/PW)** 정보를 반드시 기재해야 합니다. SMS 인증 등이 필요하다면, 심사관용으로 고정된 인증번호를 통과시키는 하드코딩된 테스트 계정을 준비해야 합니다.

### 6. 스팸 및 앱 퀄리티 (Guideline 4.3 - 2026년 업데이트)
2026년 가이드라인 업데이트로 '가치가 부족하거나 중복되는' 앱에 대한 심사가 깐깐해졌습니다. 한인회 커뮤니티 앱으로서 고유의 기능(상점/쿠폰 연동, 지역 정보 등)이 명확히 동작하도록 UI/UX 마감에 신경써야 합니다. (깨진 UI나 작동하지 않는 더미 버튼이 있으면 반려 사유가 됩니다.)

### 7. 리뷰용 가이드라인 문서 작성
본 앱은 조직(한인회 등)의 성격을 띠고 있으므로, 심사 시 "해당 단체를 대표하여 앱을 배포할 권한이 있는지" 증빙을 요구할 수 있습니다. (조직 이름으로 된 Apple Developer 계정 사용 권장).
