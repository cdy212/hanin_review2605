# Android 심사 체크 포인트 (2026 최신 기준)

본 문서는 `koreaTaiwan` 프로젝트의 현재 구현 상태를 기반으로, 최신(2026년) Google Play Store 정책에 따라 **반드시 확인 및 수정해야 하는 사항**들을 정리한 것입니다.

---

## 🚨 [필수 조치] 주요 심사 반려/삭제 예상 포인트

### 1. 데이터 삭제 정책 (Data Deletion Policy)
Google Play는 사용자 데이터 삭제에 대해 매우 엄격한 정책을 적용하고 있습니다.
앱 내부뿐만 아니라 **웹에서도 데이터를 삭제할 수 있는 별도 페이지 링크**를 제공해야 합니다.
- **현재 상태:** 앱 내에 명확한 회원 탈퇴(계정 삭제) UI가 부재하며, 삭제 처리 방침이 부족합니다.
- **필수 추가 항목:**
  1. 앱 내 탈퇴 기능
  2. 웹 브라우저에서 접근 가능한 '데이터 삭제 요청 URL' (Google Play Console 앱 콘텐츠에 등록 필수)
- **수정 대상 파일:**
  - `components/mypage/index.js` (회원 탈퇴 버튼 및 로직 추가)
  - `app/(views)/terms/` (웹으로 제공될 탈퇴 안내 또는 약관 페이지에 삭제 관련 명시)

### 2. 사용자 생성 콘텐츠(UGC) 안전 장치
커뮤니티 및 댓글 기능이 있는 앱은 Apple과 마찬가지로 구글에서도 철저히 감시합니다. 불쾌한 콘텐츠(성적인 내용, 혐오 발언 등)를 방치하면 앱이 스토어에서 즉시 정지당할 수 있습니다.
- **현재 상태:** 커뮤니티 기능(`components/community/`)에 악성 유저 차단/신고 기능 누락.
- **수정 대상 파일:**
  - `components/community/detail.js` (게시글 및 댓글에 '신고하기', '작성자 차단하기' 버튼 필수 적용)

### 3. 권한 최소화 (2026년 권한 정책 업데이트)
2026년 10월부터 위치, 연락처 등 민감한 권한에 대한 정책이 강화되었습니다. 꼭 필요한 경우가 아니면 백그라운드 위치 권한을 배제하고, '사용 중에만' 권한을 요청하도록 해야 합니다.
- **현재 상태:** 지도(`components/store/myMap.js`) 등에서 위치 권한을 사용 중입니다.
- **수정 대상 파일:**
  - `android/app/src/main/AndroidManifest.xml` (또는 `app.config.js`의 `android.permissions`)
  - 불필요한 `ACCESS_BACKGROUND_LOCATION`이 있다면 제거.
  - 꼭 필요한 `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION`만 최소한으로 요청.

---

## 🔍 [일반 조치] 유의 및 점검 사항

### 4. 최신 Android 타겟 API 레벨 준수
새로운 앱이나 업데이트를 제출할 때 항상 구글이 요구하는 최신 Android 타겟 API 레벨(일반적으로 Android 14~15 기준, API 34 이상)을 타겟팅해야 합니다.
- **조치 사항:** Expo 버전을 최신으로 유지하고, 빌드 시 경고가 나오는지 확인합니다 (`app.config.js`나 `eas.json`에 구형 SDK 버전을 강제하는지 체크).

### 5. 인앱 결제 및 대안 결제 (2026 업데이트)
앱 내에서 실물 상품이 아닌 '디지털 콘텐츠(유료 등급, 디지털 쿠폰 등)'를 판매한다면 반드시 구글 플레이 인앱 결제(Google Play Billing)를 사용해야 합니다.
- 2026년 정책에 따라 외부 결제(대안 결제)를 병행할 수 있으나 이 경우에도 구글에 수수료(약 10%)를 납부하고 신고하는 절차를 거쳐야 합니다.
- **체크 포인트:** `components/coupon`이나 `components/store`에 실결제가 붙어있는지 확인. 디지털 상품일 경우 인앱결제 연동이 필수입니다.

### 6. 개인정보처리방침 명확성
로그인(소셜 포함)과 위치 권한, 사진첩 접근 권한을 요구하는 앱이므로 개인정보처리방침에 다음 내용이 필수로 포함되어야 합니다.
1. 수집하는 정보 (이름, 이메일, 위치 정보 등)
2. 정보 수집 목적
3. 정보 보유 및 삭제 조건
- **수정 대상 파일:**
  - `components/terms/privacy.js` 및 웹용 `app/(views)/terms/privacy.js`의 내용이 구글의 정책을 충족하는지 법적 검토 필요.

### 7. 앱 접근성 및 스플래시 스크린
Android 12 이상에서 스플래시 스크린 구현 방식이 변경되었습니다. `app.config.js`에 설정된 스플래시 아이콘이 최신 안드로이드 단말에서 깨지지 않고 나오는지 테스트 기기에서 반드시 직접 확인해야 합니다.
- `android/app/src/main/res/values/styles.xml` 또는 `app.config.js`의 `expo-splash-screen` 설정 재점검.
